name: Docker Image CI

on: workflow_dispatch

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker run -ti -d --restart=always -v /srv/hnh-food:/hnh-food/frontend/api/data -p 80:8080 --name food hnh-food
